script#modal-template(type="text/x-template")
  .modal(v-show="show")
    .modal-wrapper
      content(select=".modal-header")
      content(select=".modal-content")
      content(select=".modal-footer")
        .modal-footer
